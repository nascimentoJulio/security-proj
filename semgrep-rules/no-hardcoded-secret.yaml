rules:
  - id: hardcoded-secret
    pattern: |
      const $VAR = "$SECRET";
    message: Evite segredos hardcoded no código.
    languages: [ts]
    severity: WARNING
    metadata:
      tags: [security, secret]